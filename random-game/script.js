const canvas = document.querySelector('.canvas');
const game = canvas.getContext('2d');

let area =  32;
let count = 0;
let score = 0;

/* const foods = new Image() 
foods.src = "icon/icon-avacado.png"
 */
/* let avacado = {
    x: Math.floor(Math.random(1 * 17 -1)) * area,
    y: Math.floor(Math.random(1 * 17 -1)) * area
} */